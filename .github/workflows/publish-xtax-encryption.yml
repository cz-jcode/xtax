name: Publish xtax-encryption

on:
  push:
    tags: ["xtax-encryption-v*"]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: "-D warnings"

jobs:
  dry-run:
    name: Dry-run publish check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry/
            ~/.cargo/git/
            target/
          key: ${{ runner.os }}-cargo-publish-enc-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-publish-enc-

      - name: Publish dry-run (xtax-encryption)
        run: cargo publish -p xtax-encryption --dry-run

  publish:
    name: Publish to crates.io
    runs-on: ubuntu-latest
    needs: dry-run
    environment: release
    steps:
      - uses: actions/checkout@v7

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/registry/
            ~/.cargo/git/
            target/
          key: ${{ runner.os }}-cargo-publish-enc-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-publish-enc-

      - name: Publish xtax-encryption
        run: cargo publish -p xtax-encryption --token ${{ secrets.CARGO_REGISTRY_TOKEN }}